<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".SamanApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep linking for payment redirects -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="saman" />
            </intent-filter>

            <!-- Universal Links: open thesamanapp.com URLs directly in the app.
                 Android has no "exclude" mechanism, so we whitelist allowed paths.
                 Routes intentionally omitted (stay in browser):
                   /reset-password   - email recovery must work without app installed
                   /payment/*        - Telr payment redirect targets
                   /admin            - web-only management UI
                   /checkout/*       - payment initiation needs browser context
                   /api, /objects, /uploads, /.well-known, /downloads - infra -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="thesamanapp.com" android:path="/" />
                <data android:scheme="https" android:host="thesamanapp.com" android:pathPrefix="/product/" />
                <data android:scheme="https" android:host="thesamanapp.com" android:pathPrefix="/category/" />
                <data android:scheme="https" android:host="thesamanapp.com" android:path="/categories" />
                <data android:scheme="https" android:host="thesamanapp.com" android:pathPrefix="/seller/" />
                <data android:scheme="https" android:host="thesamanapp.com" android:pathPrefix="/edit/" />
                <data android:scheme="https" android:host="thesamanapp.com" android:pathPrefix="/search" />
                <data android:scheme="https" android:host="thesamanapp.com" android:path="/sell" />
                <data android:scheme="https" android:host="thesamanapp.com" android:path="/favorites" />
                <data android:scheme="https" android:host="thesamanapp.com" android:path="/my-listings" />
                <data android:scheme="https" android:host="thesamanapp.com" android:path="/inbox" />
                <data android:scheme="https" android:host="thesamanapp.com" android:pathPrefix="/profile" />
                <data android:scheme="https" android:host="thesamanapp.com" android:path="/auth" />
                <data android:scheme="https" android:host="thesamanapp.com" android:path="/about" />
                <data android:scheme="https" android:host="thesamanapp.com" android:path="/contact" />
                <data android:scheme="https" android:host="thesamanapp.com" android:path="/help" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Android 11+ package visibility required by the TikTok Business SDK -->
    <queries>
        <package android:name="com.zhiliaoapp.musically" />
        <package android:name="com.ss.android.ugc.trill" />
    </queries>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
</manifest>
